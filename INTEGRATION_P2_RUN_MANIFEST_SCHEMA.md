# Manifest del run
Campi: `run_id`, `branch`, `commit`, `backend`, `database`, `started_at`, `finished_at`, `status`, `tests`, `created_pks` (con flag `removed`), `original_records` (con flag `restored`), `teardown_errors`, `notes`.
Il file runtime è `INTEGRATION_P2_RUN_MANIFEST_<run_id>.json`.
