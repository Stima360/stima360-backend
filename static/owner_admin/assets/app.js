const A='/api/owner/admin';async function j(p){let r=await fetch(A+p);if(!r.ok)throw Error(await r.text());return r.json()}function tab(a){if(!a.length)return'nessun dato';let k=Object.keys(a[0]).slice(0,8);return`<table><tr>${k.map(x=>`<th>${x}</th>`).join('')}</tr>${a.map(r=>`<tr>${k.map(x=>`<td>${r[x]??''}</td>`).join('')}</tr>`).join('')}</table>`}async function load(x){let d=await j('/'+x);out.innerHTML=tab(d.items)}j('/dashboard').then(d=>kpi.innerHTML=Object.entries(d).map(([k,v])=>`<div class=card><b>${v}</b><br>${k}</div>`).join(''));load('accounts');
