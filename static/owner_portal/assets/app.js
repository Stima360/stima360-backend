const A='/api/owner/portal';async function q(p,o){let r=await fetch(A+p,{credentials:'include',...o});if(!r.ok)throw Error('Risorsa non trovata');return r.status===204?{}:r.json()}async function boot(){try{await q('/auth/session');login.hidden=true;app.hidden=false;let d=await q('/properties');out.innerHTML=d.items.map(x=>`<div class=p><h2>${x.title}</h2><p>${x.address||''} ${x.city||''}</p></div>`).join('')}catch{}}go.onclick=async()=>{try{await q('/auth/token',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:token.value})});history.replaceState({},'',location.pathname);boot()}catch(e){err.textContent=e.message}};logout.onclick=async()=>{await q('/auth/logout',{method:'POST'});location.reload()};let t=new URLSearchParams(location.search).get('token');if(t){token.value=t;go.click()}else boot();
