"""A30 - l'Agenda CRM: `appointments` e' la fonte autorevole degli appuntamenti.

A30-1 porta il MODELLO e la protezione anti-sovrapposizione (vincolo di
database, lock per agente, controllo del service). Nessuna rotta: la
superficie HTTP e' A30-2, e questo pacchetto non e' montato in `main.py`.
"""
